var gulp = require('gulp');
var exec = require('child_process').exec;
var ts = require('gulp-typescript');

gulp.task('default', function (callback) {
    console.log("GggMessage: Gulp default task running.");
});
 